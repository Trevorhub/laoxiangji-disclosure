# 外部跳转链接配置说明

外部系统（小程序、App、短信、公众号菜单等）可通过 URL 参数 **门店编号**，打开 H5 后自动筛选并定位到对应门店。

## 参数规则

| 参数名 | 是否必填 | 说明 |
|--------|----------|------|
| `storeId` | 推荐 | 门店编号，与 `data.js` 中每条门店的 `id` 字段一致 |
| `store_id` | 兼容 | 与 `storeId` 等价，二选一即可 |

- 参数不区分大小写（如 `8037` 与 `8037` 相同；字母编号如 `1A26` 也不区分大小写）
- 编号需与证照数据中的门店编号完全对应，否则页面提示「未找到该门店」

## 链接格式

将 `storeId` 追加在门店资质页 URL 的查询参数中：

```
https://你的域名/stores.html?storeId=门店编号
```

### 示例

| 场景 | 链接示例 |
|------|----------|
| 直达门店资质页 | `https://example.com/stores.html?storeId=8037` |
| 从首页入口跳转（自动转发） | `https://example.com/index.html?storeId=8037` |
| 兼容写法 | `https://example.com/stores.html?store_id=1A26` |

首页 `index.html` 若携带 `storeId` / `store_id`，会自动跳转到 `stores.html?storeId=...`。

## 页面行为

1. 解析 URL 中的门店编号
2. 自动设置 **省份、城市** 筛选到该门店所在地区
3. 列表渲染后 **滚动并高亮** 目标门店卡片（约 3 秒）
4. 顶部结果文案显示：`共找到 N 家门店 · 已定位 门店名称`

若编号无效：展示「未找到该门店」及编号提示，可点击「重置筛选」恢复全量列表。

## 门店编号来源

编号来自门店证照数据表字段 **门店编号** / `store_code`，已写入前端 `data.js`：

```javascript
{
  "id": "8037",
  "province": "上海市",
  "city": "上海市",
  "name": "上海1788广场店",
  ...
}
```

外部系统配置时使用 `"id"` 的值即可。

## 集成建议

1. **统一使用 `storeId` 参数名**，便于后续维护
2. 链接使用 **HTTPS**，避免混合内容拦截
3. 发布前用真实编号在浏览器验证，例如：  
   `https://你的域名/stores.html?storeId=8037`
4. 若门店数据更新后编号变更，需同步更新外部系统中的链接

## 本地验证

```bash
cd 项目目录
python3 -m http.server 8080
```

浏览器访问：

```
http://127.0.0.1:8080/stores.html?storeId=8037
```
